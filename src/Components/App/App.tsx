import React, {useState, useEffect} from 'react'

export default function App() {
    const [totalTimeInMs, setTotalTimeInMs] = useState<number>(0);
    const [timeRunning, setTimeRunning] = useState<boolean>(false);
    return(
        <div></div>
    )
}